<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barri.horarios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".horarios_responsable"
            android:label="@string/title_activity_horarios_responsable"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".horarios_empleado"
            android:label="@string/title_activity_horarios_empleado"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
